/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as web3 from '@solana/web3.js'
import * as beetSolana from '@metaplex-foundation/beet-solana'
import * as beet from '@metaplex-foundation/beet'

/**
 * Arguments used to create {@link MetadataPermissions}
 * @category Accounts
 * @category generated
 */
export type MetadataPermissionsArgs = {
  metadata: web3.PublicKey
  user: web3.PublicKey
  canModify: boolean
}

export const metadataPermissionsDiscriminator = [
  186, 72, 170, 91, 7, 146, 107, 98,
]
/**
 * Holds the data for the {@link MetadataPermissions} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class MetadataPermissions implements MetadataPermissionsArgs {
  private constructor(
    readonly metadata: web3.PublicKey,
    readonly user: web3.PublicKey,
    readonly canModify: boolean
  ) {}

  /**
   * Creates a {@link MetadataPermissions} instance from the provided args.
   */
  static fromArgs(args: MetadataPermissionsArgs) {
    return new MetadataPermissions(args.metadata, args.user, args.canModify)
  }

  /**
   * Deserializes the {@link MetadataPermissions} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0
  ): [MetadataPermissions, number] {
    return MetadataPermissions.deserialize(accountInfo.data, offset)
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link MetadataPermissions} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey,
    commitmentOrConfig?: web3.Commitment | web3.GetAccountInfoConfig
  ): Promise<MetadataPermissions> {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    )
    if (accountInfo == null) {
      throw new Error(
        `Unable to find MetadataPermissions account at ${address}`
      )
    }
    return MetadataPermissions.fromAccountInfo(accountInfo, 0)[0]
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey(
      'AJ5Hh5q4HegZWWu1ScY7ZRA6zELXmRzEWS5EXFSKqBC6'
    )
  ) {
    return beetSolana.GpaBuilder.fromStruct(programId, metadataPermissionsBeet)
  }

  /**
   * Deserializes the {@link MetadataPermissions} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [MetadataPermissions, number] {
    return metadataPermissionsBeet.deserialize(buf, offset)
  }

  /**
   * Serializes the {@link MetadataPermissions} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return metadataPermissionsBeet.serialize({
      accountDiscriminator: metadataPermissionsDiscriminator,
      ...this,
    })
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link MetadataPermissions}
   */
  static get byteSize() {
    return metadataPermissionsBeet.byteSize
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link MetadataPermissions} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    connection: web3.Connection,
    commitment?: web3.Commitment
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(
      MetadataPermissions.byteSize,
      commitment
    )
  }

  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link MetadataPermissions} data.
   */
  static hasCorrectByteSize(buf: Buffer, offset = 0) {
    return buf.byteLength - offset === MetadataPermissions.byteSize
  }

  /**
   * Returns a readable version of {@link MetadataPermissions} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      metadata: this.metadata.toBase58(),
      user: this.user.toBase58(),
      canModify: this.canModify,
    }
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const metadataPermissionsBeet = new beet.BeetStruct<
  MetadataPermissions,
  MetadataPermissionsArgs & {
    accountDiscriminator: number[] /* size: 8 */
  }
>(
  [
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['metadata', beetSolana.publicKey],
    ['user', beetSolana.publicKey],
    ['canModify', beet.bool],
  ],
  MetadataPermissions.fromArgs,
  'MetadataPermissions'
)
